# StarMark 重构路线（代码洞察实施计划）

> 依据：`docs/代码洞察报告.md`（2026-09-18）。
> 状态标记：⬜ 未开始 ｜ 🔵 进行中 ｜ ✅ 完成 ｜ ⏸️ 有意推迟（附理由）。
> 原则：每完成一项跑全量测试（vitest + tsc + build），通过后提交；纯移动不改逻辑，行为变更单独提交。

---

## P1 · 消灭最大技术债

- ✅ **F1 options 按 panel 拆分**（App.tsx 955 → 68 行组装层 + 7 个自治面板；realbrowser 冒烟通过并顺带修复 browser.close()/rmSync 偶发挂起）✅ `70fa07d`
  - `panels/AiPanel.tsx`（AI 整理：设置/运行/暂停/分组应用/导出导入/清理，完全自治）
  - `panels/RulesPanel.tsx`（规则自动标签，自治）
  - `panels/AppearancePanel.tsx`（主题/字母头像/语言/右键菜单/快捷键，自治）
  - `panels/AccountPanel.tsx`（PAT/同步频率/立即同步/重建索引，接收 state + setState + refresh）
  - `panels/StatsPanel.tsx`（统计 + 健康度展示，接收 state + health）
  - `panels/DataPanel.tsx`（备份导出导入 / 通用导出 / 清除全部，接收 notify + refresh）

## P2 · 可测性与复用

- ✅ **D1/D2 搜索纯函数抽取** `ea7b80e` 内完成
  - `core/search/filters.ts`：来源/隐藏/标签三类谓词（worker 与 BrowseNode 共用）
  - `core/search/query.ts`：browseItems / literalFallback / enrichHits / itemToHit / buildFolderNodeTree 纯函数；worker 只剩 MiniSearch 调用与消息分发；新增 8 项单测
- ⏸️ **B1+C3 作业骨架（job-runner）**——**有意推迟**。三个状态机（github 分页爬取 / classify 批次 / pipeline 游标）语义差异大于共性（共性仅防重入 + 检查点落盘，各约 20 行），抽象收益低于间接层成本；且三者均已稳定并有测试覆盖。**重新触发条件**：Phase C 新作业（Lists 同步 / 向量索引）落地时，以新作业为样本抽取 `createLongTask()` 骨架并回迁三者。
- ✅ **C4 错误类型化（示范版）**：`core/errors.ts`（AppError 基类 + AiConfigError / AiRateLimitError / OllamaOriginError / OllamaConnectError / AbortedError），AI 域高频抛错点已接入且 message 内容零变化（行为零回归）；UI 按 `e.code` 程序化识别的渲染改造与剩余抛错点（api/github 10 处、backup/convert）随迭代跟进
- ✅ **3.2-1 options 健康报告缓存**（indexVersion + 语言键，重复打开零开销）
- ✅ **3.2-2 doSearch 省 bulkGet**（命中补全优先走内存条目缓存，仅缓存不可用回退 bulkGet）

## P3 · 常量收敛与防回归

- ✅ **3.1 constants 收敛** `9e1d9f3`：`core/constants.ts`（DB_BULK_CHUNK / AI_SUGGEST_PAGE_SIZE / UI_POLL_MS / INVALIDATE_DEBOUNCE_MS / KEEPALIVE_MS / ZOMBIE_AFTER_MS / SNAPSHOT_DEBOUNCE_MS / ACTIVITY_* / DEFAULT_SYNC_HOURS / BM_WALK_THROTTLE_MS / BROWSER_CLOSE_GUARD_MS），9 个文件 11 处字面量迁移
- ✅ **G3 收藏夹 id 化**：`ensureCollectFolder` 首次创建后把 folder id 记入 storage（`collectFolderId`），按 id 定位；id 失效（文件夹被删）按名字回退并重建映射——用户改名不再产生重复文件夹
- ✅ **D4 快照版本头**：核实 `SearchIndexRecord.version` 与 `ensureIndex` 的 `saved.version === indexVersion` 校验**已覆盖**该需求（版本不符直接全量重建），无需额外工作
- ✅ **G2 热榜降级可见化**：`fetchTrending` 返回 `via`（trending-html / search-api），Search API 兜底时 UI 顶部标注三语提示——解析坏掉不再静默
- ✅ **C4 附带**：`normalizedTitle` 死代码删除（标题判重语义废弃），相关测试重写为 URL 判重断言
- ⬜ **G1 响应类型同构**：BgResponseMap 与 BgRequest 同构（中等工作量，收益是编译期兜住漏填字段）
- ⬜ **A2 迁移基线压缩预研**：v7 检测 `meta.schemaBaseline` 跳过历史迁移（不急，用户量小）
- ⬜ **SearchSourceRegistry 预研**：Phase C 前置债，来源枚举 5 处硬编码的注册表方案（只出设计不实施）

## 完成定义

每项：全量测试通过（vitest + `tsc --noEmit` + `wxt build`）→ git 提交（拆分/修复分开）→ 推送。UI 拆分类另需 realbrowser 冒烟通过。

## 执行日志

- `70fa07d` P1 options 拆面板（955 → 68 行 + 7 面板）+ realbrowser close/rmSync 加固
- `ea7b80e` P2 性能两项（doSearch 缓存补全、健康报告缓存）+ render 冒烟抓出 useRef 缺 import
- `9e1d9f3` P3 constants 收敛（11 处）
- （本提交）D1/D2 搜索纯函数 + query.test（8 项）、C4 错误示范版、G2/G3、D4 核实豁免、normalizedTitle 死代码清理
